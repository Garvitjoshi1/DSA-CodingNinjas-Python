e = (5, 6)
print(e[0], e[1])  # or
print(e[-1], e[-2])  # to access elements from last
e = (1, 2, 3, 4, 5)
print(e[0:3])  # slicing is same as of list/arr.
# Also, tuples are immutable, i.e, append remove update the tuple is not valid
# e[0] = 5 will give error
